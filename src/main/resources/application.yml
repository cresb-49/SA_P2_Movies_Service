spring:
  application:
    name: movies-service
  datasource:
    url: jdbc:postgresql://localhost:5432/movies_service
    username: postgres
    password: 201931012
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration
  profiles:
    active: local
  config:
    import: "optional:configserver:http://localhost:8888"
